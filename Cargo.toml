[package]
name = "TurtleAPIServer"
version = "0.1.0"
edition = "2024"

[dependencies]
fs = "0.0.6"
tokio = { version = "1.0", features = ["full"]}
sqlx = { version = "0.9", features = ["runtime-tokio", "postgres", "macros", "tls-native-tls"]} 
dotenvy = "0.15"
